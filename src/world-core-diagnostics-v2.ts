import type { World } from './engine';
import { deterministicRandom,queueWorldEvent,restoreWorldCore,snapshotWorldCore,syncWorldDate,validateWorldCore,worldCore,worldEvents } from './world-core-v2';
export type WorldCoreCheck={name:string;ok:boolean;detail:string};
export function worldCoreDiagnosticsV2(world:World){const s=worldCore(world),checks:WorldCoreCheck[]=[],a=deterministicRandom(world,'world','diagnostic'),b=deterministicRandom(world,'world','diagnostic');checks.push({name:'stable RNG',ok:a===b,detail:`${a.toFixed(6)} / ${b.toFixed(6)}`});const snap=snapshotWorldCore(world),id=queueWorldEvent(world,{date:s.date,type:'DiagnosticEvent',scope:'world',entityIds:[],importance:1,payload:{test:true}});checks.push({name:'event queue',ok:worldEvents(world,{type:'DiagnosticEvent'}).some(e=>e.id===id),detail:`evento ${id}`});const cadence=syncWorldDate(world,s.date);checks.push({name:'daily cadence',ok:cadence.cadences.includes('daily'),detail:cadence.cadences.join(',')});restoreWorldCore(world,snap);const restored=snapshotWorldCore(world);checks.push({name:'snapshot restore',ok:restored.worldId===snap.worldId&&restored.seed===snap.seed&&restored.events.length===snap.events.length,detail:`${restored.worldId}, ${restored.events.length} eventos`});const valid=validateWorldCore(world);checks.push({name:'invariants',ok:valid.ok,detail:valid.issues.join('; ')||'ok'});return{ok:checks.every(c=>c.ok),checks,state:valid}}
