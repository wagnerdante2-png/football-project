import type { MatchCoreState } from './match-core-v2';
import { lifecycleEvents } from './match-lifecycle-v2';
import { goalFrameHitLog } from './match-ball-physics-v2';
import { shotResolutionLog } from './match-goalkeeper-resolution-v2';

export function lifecyclePhysicsDiagnostics(state:MatchCoreState){const life=lifecycleEvents(state),frame=goalFrameHitLog(state),shots=shotResolutionLog(state),shotFrame=shots.filter(s=>s.frameHit),types=Object.fromEntries(['leftPost','rightPost','crossbar'].map(k=>[k,frame.filter(x=>x.kind===k).length+shotFrame.filter(x=>x.frameHit===k).length]));const hasKickoff=life.some(e=>e.type==='kickoff'),hasHalf=life.some(e=>e.type==='halftimeWhistle'),hasSecond=life.some(e=>e.type==='secondHalfKickoff'),hasFull=life.some(e=>e.type==='fulltimeWhistle'),issues:string[]=[];if(!hasKickoff)issues.push('Partida sem evento formal de pontapé inicial.');if(state.phase==='finished'&&!hasFull)issues.push('Partida encerrada sem apito final formal.');if(state.seconds>3000&&!hasHalf)issues.push('Primeiro tempo transcorrido sem evento de intervalo.');if(state.seconds>3300&&!hasSecond)issues.push('Segundo tempo iniciado sem reinício formal.');if(state.pitch.goalWidth!==7.32||state.pitch.goalHeight!==2.44)issues.push('Dimensões da meta fora do padrão esperado 7,32 x 2,44 m.');return{ok:issues.length===0,issues,lifecycleEvents:life.length,goalFrameHits:frame.length+shotFrame.length,frameByType:types,hasKickoff,hasHalf,hasSecond,hasFull}}
