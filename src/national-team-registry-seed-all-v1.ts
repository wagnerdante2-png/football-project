import type { World } from './engine';
import { alias, registerAssociation, registerNationalTeam } from './world-football-data-v1';
import { registerCountryProfile } from './world-geography-climate-v2';
import { GLOBAL_NATIONAL_TEAM_SEEDS, HISTORICAL_NATIONAL_IDENTITIES } from './national-team-registry-v1';
import { EXTENDED_NATIONAL_TEAM_SEEDS } from './national-team-registry-extended-v1';
const provenance={sourceId:'football-project-national-team-registry',license:'internal',historical:false,confidence:100};
export function seedAllNationalTeams(world:World){let associations=0,nationalTeams=0;const all=[...GLOBAL_NATIONAL_TEAM_SEEDS,...EXTENDED_NATIONAL_TEAM_SEEDS],seen=new Set<string>();for(const n of all){if(seen.has(n.id))continue;seen.add(n.id);registerCountryProfile(world,{id:n.id,name:n.name,continent:n.continent,associationId:n.associationId,hemisphere:n.hemisphere,defaultClimate:n.climate});registerAssociation(world,{id:n.associationId,name:n.associationName,countryId:n.id,confederationId:n.confederationId,aliases:[alias(n.associationId)],provenance:[provenance]});registerNationalTeam(world,{id:`nt-${n.id.toLowerCase()}-men-senior`,countryId:n.id,associationId:n.associationId,name:n.name,aliases:n.aliases.map(x=>alias(x)),teamKind:'senior',gender:'men',active:true,provenance:[provenance]});associations++;nationalTeams++}for(const h of HISTORICAL_NATIONAL_IDENTITIES){registerAssociation(world,{id:h.associationId,name:`Historical association: ${h.name}`,countryId:h.id,confederationId:h.confederationId,aliases:[alias(h.associationId)],provenance:[{...provenance,historical:true}]});registerNationalTeam(world,{id:`nt-${h.id.toLowerCase()}-men-senior`,countryId:h.id,associationId:h.associationId,name:h.name,aliases:h.aliases.map(x=>alias(x,{validFrom:h.validFrom,validTo:h.validTo})),teamKind:'senior',gender:'men',active:false,provenance:[{...provenance,historical:true}]});nationalTeams++}return{associations,nationalTeams,historical:HISTORICAL_NATIONAL_IDENTITIES.length,current:seen.size}}
